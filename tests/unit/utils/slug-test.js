import slug from 'leap-dashboard/utils/slug';
import { module, test } from 'qunit';

module('Unit | Utility | slug');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = slug();
  assert.ok(result);
});
