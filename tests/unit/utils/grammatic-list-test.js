import grammaticList from 'lead-dashboard/utils/grammatic-list';
import { module, test } from 'qunit';

module('Unit | Utility | grammatic list');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = grammaticList();
  assert.ok(result);
});
