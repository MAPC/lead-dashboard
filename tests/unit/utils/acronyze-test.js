import acronyze from 'lead-dashboard/utils/acronyze';
import { module, test } from 'qunit';

module('Unit | Utility | acronyze');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = acronyze();
  assert.ok(result);
});
