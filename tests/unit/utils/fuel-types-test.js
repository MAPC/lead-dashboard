import fuelTypes from 'leap-dashboard/utils/fuel-types';
import { module, test } from 'qunit';

module('Unit | Utility | fuel types');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fuelTypes();
  assert.ok(result);
});
