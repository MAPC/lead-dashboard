import maxRowByKey from 'lead-dashboard/utils/max-row-by-key';
import { module, test } from 'qunit';

module('Unit | Utility | max row by key');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = maxRowByKey();
  assert.ok(result);
});
