import maps from 'leap-dashboard/utils/maps';
import { module, test } from 'qunit';

module('Unit | Utility | maps');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = maps();
  assert.ok(result);
});
