import capitalize from 'leap-dashboard/utils/capitalize';
import { module, test } from 'qunit';

module('Unit | Utility | capitalize');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = capitalize();
  assert.ok(result);
});
