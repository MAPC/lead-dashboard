import sectors from 'leap-dashboard/utils/sectors';
import { module, test } from 'qunit';

module('Unit | Utility | sectors');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sectors();
  assert.ok(result);
});
